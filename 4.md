#include<stdio.h>
float Add(float a, float b);
int main(){
    float a = 10.0, b = 20.0;
    float result;
    result = Add(a, b);
    printf("%.1f\n", result);
    return 0;
}
float Add(float a, float b){
    return a + b;
}
